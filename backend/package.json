{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "typeorm": "ts-node-dev ./node_modules/typeorm/cli.js",
    "migration:generate": "npm run typeorm migration:generate -- -d ./src/infrastructure/database/data-source.ts -n",
    "migration:run": "npm run typeorm migration:run -d ./src/infrastructure/database/data-source.ts",
    "migration:revert": "npm run typeorm migration:revert -d ./src/infrastructure/database/data-source.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^4.17.21",
    "bcrypt": "^5.1.1",
    "express": "^4.19.2",
    "typeorm": "^0.3.20"
  }
}
